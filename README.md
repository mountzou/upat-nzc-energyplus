## Configuration

- **`MEASUREMENT_TZ_OFFSET_HOURS`** (default: `2`): Hours added to all measurement timestamps (stored without timezone) so that the app shows times in the user's local timezone. Set via environment variable.
